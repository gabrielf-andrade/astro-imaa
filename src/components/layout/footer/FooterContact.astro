---
import WhatsAppIcon from "@/components/icons/WhatsAppIcon";
import type { QueryContactInfo } from "@/lib/sanity-derived-types";
import { Mail, MapPin, Phone } from "@lucide/astro";

interface Props {
  contactInfo: QueryContactInfo;
}

const { contactInfo } = Astro.props;
---

<div>
  <h3 class="heading-label mb-4">Contato</h3>
  <ul class="space-y-3">
    {
      contactInfo.address && (
        <li class="flex items-start gap-2">
          <MapPin class="w-4 h-4 text-secondary mt-0.5 shrink-0" />
          <div class="caption-text-primary-foreground">
            {contactInfo.address.street && (
              <p>
                {contactInfo.address.street}
                {contactInfo.address.complement && `, ${contactInfo.address.complement}`}
              </p>
            )}
            {contactInfo.address.neighborhood && (
              <p>
                {contactInfo.address.neighborhood}
                {contactInfo.address.cityState && ` - ${contactInfo.address.cityState}`}
              </p>
            )}
            {contactInfo.address.zipCode && <p class="text-primary-foreground/60">{contactInfo.address.zipCode}</p>}
          </div>
        </li>
      )
    }

    {
      contactInfo.emails
        ?.filter((item) => item.address)
        .map((item) => (
          <li class="flex items-center gap-2">
            <Mail class="w-4 h-4 text-secondary shrink-0" />
            <a
              href={`mailto:${item.address}`}
              class="caption-text-primary-foreground hover:text-secondary transition-colors break-all"
            >
              {item.address}
            </a>
          </li>
        ))
    }

    {
      contactInfo.phones
        ?.filter((item) => item.number)
        .map((item) => (
          <li class="flex items-center gap-2">
            {item.isWhatsApp ? (
              <WhatsAppIcon className="w-5 h-5 text-secondary shrink-0" />
            ) : (
              <Phone class="w-5 h-5 text-secondary shrink-0" />
            )}
            <a
              href={item.isWhatsApp ? `https://wa.me/${item.number?.replace(/\D/g, "")}` : `tel:${item.number}`}
              target={item.isWhatsApp ? "_blank" : undefined}
              rel={item.isWhatsApp ? "noopener noreferrer" : undefined}
              class="caption-text-primary-foreground hover:text-secondary transition-colors"
            >
              {item.number}
            </a>
          </li>
        ))
    }
  </ul>
</div>
